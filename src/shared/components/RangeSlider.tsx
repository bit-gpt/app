import VendorRangeSlider from "react-range-slider-input";
import "react-range-slider-input/dist/style.css";

const RangeSlider = (props: typeof VendorRangeSlider) => {
  return <VendorRangeSlider {...props} />;
};

export default RangeSlider;
