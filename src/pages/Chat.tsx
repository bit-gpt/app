import { useState } from "react";
import { invoke } from "@tauri-apps/api/tauri";

function Chat() {
  return (
    <div className="container">
        <h1>Prem Chat</h1>
        <h2>Here the chat interface.</h2>
    </div>
  );
}

export default Chat;
